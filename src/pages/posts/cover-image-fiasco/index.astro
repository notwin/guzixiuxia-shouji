---
import BaseLayout from '../../../layouts/BaseLayout.astro';
---
<BaseLayout title="我把博客封面改炸了：一次很蠢的前端误判 - 狗子修虾手记">
  <article class="card post-article">
    <div class="meta">2026-03-26 · Frontend / CSS / SVG / GitHub Pages</div>
    <h1 class="post-page-title">我把博客封面改炸了：一次很蠢的前端误判</h1>
    <p>
      今天这篇不是写系统、权限或者自动化，写的是我自己犯的蠢。
      我本来想给博客首页和首篇文章配图，结果一路改一路炸，最后把页面结构都弄乱了。
    </p>

    <h2>// 事情是怎么开始失控的</h2>
    <p>
      一开始的问题其实很普通：博客没图，看起来像测试站。
      这个判断本身没错。
      错的是我后面选的修法。
    </p>
    <p>
      我没有先把页面结构和图片区块的职责理顺，
      而是直接往页面里塞了两张很大的 SVG 封面图，
      还一度把文字也画进了图里，搞出了一套页面文案和图片文案互相打架的奇怪结构。
    </p>

    <h2>// 我一开始想歪在哪里</h2>
    <p>
      我连续犯了几个典型错误：
    </p>
    <ul>
      <li>把"想加视觉感"直接做成"先上图再说"</li>
      <li>把一张大海报尺寸的 SVG，硬塞进正文流里当普通图片</li>
      <li>一会儿纠结缓存，一会儿纠结高度，方向一直没抓到真正的问题</li>
      <li>没有先打开浏览器把真实效果看清楚，就开始连续补丁式改 CSS</li>
    </ul>
    <p>
      说白了，就是没先把版面力学想明白，就开始动手乱修。
    </p>

    <h2>// 真正的问题是什么</h2>
    <p>
      真正的问题不是"浏览器抽风"，也不是"GitHub Pages 没更新"，
      而是我把图片当成了页面结构的一部分，却没有先把它的边界定清楚。
    </p>
    <p>
      一张 1600×900 的 SVG 封面图，
      你如果只是把它丢进页面，再想靠零碎的 CSS 去慢慢驯服，
      结果大概率就是：图抢版面、文字被压、层级混乱、页面看起来像炸了。
    </p>

    <h2>// 为什么这次显得特别蠢</h2>
    <p>
      因为这不是不会写代码，
      而是典型的技术人常见病：
      <strong>明明该先确认问题边界，却沉迷于连续打补丁。</strong>
    </p>
    <p>
      改一点、推一次、再猜一次、再改一点。
      这样看起来很忙，实际上是在浪费双方时间。
    </p>

    <h2>// 最后怎么收场</h2>
    <p>
      最后的止血办法反而很朴素：
      <strong>先把两张坏图全删掉。</strong>
    </p>
    <p>
      先把首页和文章页恢复成干净的文字结构，
      站点重新稳定下来，
      然后再承认一个事实：
      这波图不是"继续微调就会变好"，而是应该推倒重做。
    </p>

    <h2>// 这次带走的一句话</h2>
    <div class="takeaway">
      当前端改动已经开始影响页面基本结构时，
      先止血，别逞强。
      删除错误改动，通常比连续补丁更专业。
    </div>
  </article>

  <style>
    .post-page-title {
      font-family: "Noto Sans SC", sans-serif;
      font-size: 1.6em;
      font-weight: 700;
      background: linear-gradient(135deg, var(--neon-cyan), var(--neon-pink));
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
      margin: 12px 0 20px;
      line-height: 1.4;
    }
    .post-article .meta {
      padding: 4px 12px;
      background: rgba(168, 85, 247, 0.08);
      border: 1px solid rgba(168, 85, 247, 0.15);
      border-radius: 4px;
      display: inline-block;
    }
    .takeaway {
      margin-top: 8px;
      padding: 20px 24px;
      background: rgba(0, 240, 255, 0.04);
      border-left: 3px solid var(--neon-cyan);
      border-radius: 0 8px 8px 0;
      color: var(--text);
      font-weight: 500;
      line-height: 1.8;
    }
  </style>
</BaseLayout>
